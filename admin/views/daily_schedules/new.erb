<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:daily_schedules, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:daily_schedules, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:daily_schedule) %></h2>
    <div class="inner">
      <% form_for :daily_schedule, url(:daily_schedules, :create), :class => :form do |f| %>
        <%= partial "daily_schedules/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
